<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.InternshipOnline2nd" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/blue1</item>
        <item name="fontFamily">@font/andika</item>
    </style>

    <style name="Theme.InternshipOnline2nd" parent="Base.Theme.InternshipOnline2nd" />

    <style name="edit_16" parent="Widget.AppCompat.EditText">
        <item name="android:textSize">@dimen/_16sdp</item>
        <item name="android:layout_marginHorizontal">@dimen/_20sdp</item>
        <item name="android:background">@drawable/custom_border</item>
        <item name="android:padding">@dimen/_10sdp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textColorHint">@color/white_light</item>
    </style>

    <style name="edit_16_without_border" parent="Widget.AppCompat.EditText">
        <item name="android:textSize">@dimen/_16sdp</item>
        <item name="android:padding">@dimen/_10sdp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textColorHint">@color/white_light</item>
    </style>

</resources>